name: robotsix_chat_mobile
description: Mobile client for robotsix-chat — a conversational agent service.
publish_to: none
version: 0.3.0+4

environment:
  sdk: ^3.10.0
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  package_info_plus: ^10.2.1
  path_provider: ^2.1.0
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.3.0
  url_launcher: ^6.3.0
  app_links: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
